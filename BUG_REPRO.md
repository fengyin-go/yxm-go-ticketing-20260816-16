# BUG_REPRO

## Bug 是什么
工单取消分配客服时，已进入处理中的工单不会回到待处理状态；删除客服时也没有阻止删除仍被工单分配的客服。

## 如何触发
1. 创建客服和工单。
2. 调用 `PATCH /api/tickets/{id}/assign`，把客服分配给工单，工单变为 `processing`。
3. 再次调用同一接口并传入空 `assignee_id` 取消分配。
4. 调用 `DELETE /api/agents/{id}` 删除该客服。

## 错误信息
- 取消分配后工单状态仍为 `processing`，预期应回到 `open`。
- 删除仍被工单分配的客服时返回 `200`，预期应返回 `409`。
