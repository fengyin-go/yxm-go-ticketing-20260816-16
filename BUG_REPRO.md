# BUG_REPRO

## Bug 是什么
工单列表的关键词搜索只匹配标题，不匹配描述、请求人等信息；分页参数解析和切片起点都有错误。

## 如何触发
1. 创建多个工单，其中一个描述包含唯一关键词，并设置相同优先级。
2. 请求 `GET /api/tickets?keyword=<描述关键词>`。
3. 请求 `GET /api/tickets`（省略分页参数）。
4. 请求 `GET /api/tickets?size=1000`。

## 错误信息
- 用描述关键词搜索时返回 `total: 0`，预期命中对应工单。
- 省略分页参数时返回的 `pagination.size` 为 `0`。
- 省略分页参数时返回的条目数量比预期少。
- 传超大 `size` 时返回的 `pagination.size` 没有被限制到最大页大小。
