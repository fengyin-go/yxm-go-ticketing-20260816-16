# BUG_REPRO

## Bug 是什么
工单更新接口没有校验目标分类是否存在；删除分类接口也没有检查该分类是否仍被工单引用，导致出现指向不存在分类的工单。

## 如何触发
1. 先创建一个分类和一个引用该分类的工单。
2. 调用 `PUT /api/tickets/{id}`，把 `category_id` 改成不存在的值。
3. 再调用 `DELETE /api/categories/{id}`，删除仍被工单引用的分类。

## 错误信息
- 更新到不存在分类时接口返回 `200`，而不是 `404`。
- 删除仍被使用的分类时接口返回 `200`，而不是 `409`。
